
public enum Team
{
    Player,
    Enemy,
    Neutral
}

public enum ActorMoveState
{
    Idle,
    Moving
}

public enum From
{
    Above,
    Right,
    Below,
    Left
}

public enum Direction
{
    None,
    Up,
    Right,
    Down,
    Left
}
