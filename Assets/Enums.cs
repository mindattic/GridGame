
public enum Team
{
    Player,
    Enemy,
    Neutral
}

public enum ActorState
{
    Idle,
    Active,
    Moving
}

public enum From
{
    Above,
    Below,
    Right,
    Left
}