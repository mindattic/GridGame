using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : ExtendedMonoBehavior
{
    // Play is called before the first frame update
    void Start()
    {
        
    }

    // SaveProfile is called once per frame
    void Update()
    {
        
    }
}
