using UnityEngine;

public class Quality
{
    public string Name;
    public Color Color;

    public Quality(string name, Color color)
    {
        Name = name;
        Color = color;
    }
}